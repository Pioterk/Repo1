import { AssignStrategyTextNotificationToReport } from './assign-strategy-text-notification-to-report';

describe('AssignStrategyTextNotificationToReport', () => {
  it('should create an instance', () => {
    expect(new AssignStrategyTextNotificationToReport()).toBeTruthy();
  });
});
